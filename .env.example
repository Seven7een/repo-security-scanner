# GitHub
GITHUB_USERS=your-github-username
GITHUB_TOKEN=

# GitLab (Phase 2)
GITLAB_USERS=
GITLAB_TOKEN=

# AI scanning (Phase 2) — required for --ai or --all
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
BEDROCK_MODEL_ID=us.anthropic.claude-sonnet-4-20250514

# Email reporting (Phase 2) — required for --email
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
ALERT_EMAIL=
